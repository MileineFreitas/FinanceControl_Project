@{
    var path = (ViewContext.HttpContext.Request.Path.Value ?? "").Trim('/').ToLowerInvariant();
    var cadastrosOpen = path.StartsWith("categorias", StringComparison.Ordinal)
        || path.StartsWith("transacoes", StringComparison.Ordinal)
        || path.StartsWith("transactions", StringComparison.Ordinal);
    var dashboardsOpen = path.StartsWith("dashboards", StringComparison.Ordinal);
    var isHome = path is "home" or "";
    var openCad = isHome || cadastrosOpen;
    var openDash = isHome || dashboardsOpen;
}
<div class="nav-root" id="fc-nav-root">
    <nav class="nav-menu px-2 pt-2 pb-2" aria-label="Navegação principal">
        <button type="button"
                class="nav-collapse-btn nav-link w-100 mb-2 border-0"
                data-fc-sidebar-toggle
                aria-label="Recolher ou expandir menu lateral">
            <svg class="nav-icon" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z" fill="currentColor" />
            </svg>
            <span class="nav-label">Recolher</span>
        </button>

        <a class="nav-link w-100" href="~/home">
            <img class="nav-icon" src="~/icon-home.png" alt="" />
            <span class="nav-label">Home</span>
        </a>

        <details class="nav-group fc-nav-details" @(openCad ? "open" : null)>
            <summary class="nav-link nav-group-toggle w-100 text-start">
                <img class="nav-icon" src="~/icon-menu.png" alt="" />
                <span class="nav-label">Cadastros</span>
                <span class="nav-chevron open" aria-hidden="true"></span>
            </summary>
            <div class="nav-sub">
                <a class="nav-link nav-sublink w-100" href="~/categorias">
                    <span class="nav-sublink-text">Categorias</span>
                </a>
                <a class="nav-link nav-sublink w-100" href="~/transacoes">
                    <span class="nav-sublink-text">Transações</span>
                </a>
            </div>
        </details>

        <details class="nav-group fc-nav-details" @(openDash ? "open" : null)>
            <summary class="nav-link nav-group-toggle w-100 text-start">
                <img class="nav-icon" src="~/icon-relatorio.png" alt="" />
                <span class="nav-label">Dashboards</span>
                <span class="nav-chevron open" aria-hidden="true"></span>
            </summary>
            <div class="nav-sub">
                <a class="nav-link nav-sublink w-100" href="~/dashboards/geral">
                    <span class="nav-sublink-text">Geral</span>
                </a>
                <a class="nav-link nav-sublink w-100" href="~/dashboards/por-categoria">
                    <span class="nav-sublink-text">Por categoria</span>
                </a>
                <a class="nav-link nav-sublink w-100" href="~/dashboards/por-transacoes">
                    <span class="nav-sublink-text">Por transações</span>
                </a>
            </div>
        </details>
    </nav>
</div>
